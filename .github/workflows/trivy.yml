name: Trivy

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 6 * * *"  # Daily at 06:00 UTC

jobs:
  scan:
    name: Full Repository Scan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Initialize submodules
        run: git submodule update --init --recursive

      - name: Setup Trivy
        uses: aquasecurity/trivy-action@0.28.2
        with:
          scan-type: fs
          scan-ref: "."
          scanners: secret,misconfig
          secret-config: trivy-secret.yaml
          severity: CRITICAL,HIGH,MEDIUM
          format: table
          exit-code: 1
          ignore-unfixed: true
